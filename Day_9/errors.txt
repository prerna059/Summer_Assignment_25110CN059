Initialization of a parameter variable(eg char) placement inside or outside the loop matters.
1. outside will update inside BOTH the loops.
2. inside the first loop will have the variable reset to the same value after every round of updation of the parameter variable inside nested loop.
